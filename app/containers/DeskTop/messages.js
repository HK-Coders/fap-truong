/*
 * DeskTop Messages
 *
 * This contains all the text for the DeskTop container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DeskTop';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeskTop container!',
  },
});
